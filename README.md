# Clinical Planning System

**Планирование и анализ лицевой хирургии на базе AI**

> Веб-приложение для клинического анализа лица, планирования пластических операций и документирования результатов. Работает в браузере — без установки.

---

## Запуск

### Онлайн
https://luna-huker36.github.io/Clinical-Planning-System/

### Локально
- **macOS** — двойной клик на `start.command`
- **Windows** — двойной клик на `start.bat`

> Требуется Python 3 и современный браузер (Chrome, Edge, Firefox).

### Локально с 3D-реконструкцией (рекомендуется)
- Установите зависимости: `npm install`
- Запустите backend: `npm start`
- Откройте `http://localhost:3000/` — фронтенд сам обнаружит backend и включит реальный движок

> `start.command` / `start.bat` теперь сами запускают Node-бэкенд, если Node.js установлен.
> Принудительно переключить режим: `?reconstructionMode=backend` или `?reconstructionMode=mock`.

---

## Инструкция: 2D Анализ

### 1. Загрузка фото
- Откройте вкладку **«2D Анализ»**
- Нажмите **«Загрузить фото»** и выберите фронтальный снимок лица (JPG/PNG)
- Фото появится на рабочей области

### 2. AI-анализ
- Нажмите **«AI ориентиры»** — система автоматически найдёт ключевые точки лица
- На фото появятся: средняя линия, горизонталь глаз, три трети лица
- В панели отобразятся: индекс гармонии, симметрия, пропорции, отклонение носа

### 3. Калибровка
- Нажмите **«Калибровка»**
- Укажите две точки на фото с известным расстоянием между ними
- Введите реальное расстояние в мм — все дальнейшие измерения будут в миллиметрах

### 4. Ручные инструменты
- **Расстояние** — кликните 2 точки → покажет расстояние (H, L и AB = √(H² + L²))
- **Вектор** — кликните 2 точки → стрелка направления перемещения тканей
- **Наклон** — кликните 2 точки → угол наклона относительно горизонта
- **Угол** — кликните 3 точки → клинический угол между отрезками
- **Зона** — обведите область → полигон с расчётом площади

### 5. Готовые пресеты
Нажмите **«Пресеты»** и выберите один из готовых наборов:
- **Кантинг глаз** — наклон линии глаз
- **Ось носа** — наклон от glabella до кончика носа
- **Носогубный угол** — 3-точечный угол (columella → subnasale → верхняя губа)
- **Линия подбородка** — наклон нижней трети
- **Фейслифтинг** — базовые векторы, SMAS plication, Deep-plane, MACS

### 6. Асимметрия
- Система автоматически сравнивает парные элементы (R vs L)
- Показывает разницу в длине, угле, площади и смещении

### 7. До / После
- Нажмите **«Сохранить До»** — запомнит текущее состояние
- Внесите изменения в план
- Нажмите **«Показать До»** — наложит сохранённый снимок для сравнения

### 8. Экспорт
- **PDF** — нажмите кнопку «PDF» → скачается отчёт со скриншотом и всеми измерениями
- **DOCX** — нажмите кнопку «DOCX» → скачается документ для медкарты

---

## Инструкция: 3D Модель

### 1. Выбор модели
- Откройте вкладку **«3D Модель»**
- Выберите модель из списка (Lee Perry-Smith или Face Cap)
- Или загрузите свой файл `.glb` / `.gltf` кнопкой **«Загрузить»**

### 2. Навигация
- **ЛКМ + перетаскивание** — вращение модели
- **ПКМ + перетаскивание** — перемещение
- **Колесо мыши** — масштаб
- **Сброс вида** — вернуть камеру в исходное положение

### 3. Режимы отображения
- **Wireframe** — каркасный вид (сетка полигонов)
- **Нормали** — цветная карта направлений поверхности
- **Свет**: Дневной / Мягкий / Студия — три варианта освещения

### 4. Калибровка
- Нажмите **«Калибровка»**
- Кликните 2 точки на модели с известным расстоянием
- Введите реальное расстояние в мм

### 5. Инструменты (кликайте прямо по поверхности модели)
- **Точка** — клик на модель → ставит маркер с координатами
- **Расстояние** — 2 клика → линия + расстояние в мм
- **Вектор** — 2 клика → стрелка направления + длина
- **Угол** — 3 клика → угол между двумя отрезками в градусах
- **Наклон** — 2 клика → угол наклона относительно горизонтальной плоскости
- **Измерение** — 2 клика → линия + расстояние (универсальная линейка)

### 6. План операции
- Все измерения автоматически добавляются в список **«Элементы плана»** слева
- Каждый элемент можно удалить кнопкой **«Удалить»**
- Кнопка **«Очистить всё»** — сбросить все аннотации

### 7. Экспорт
- **PDF** — скриншот 3D-модели с аннотациями + список измерений
- **DOCX** — структурированный документ с данными

---

## Движок 3D-реконструкции (PMAS Native Engine)

Собственный движок реконструкции на чистом JavaScript (Node.js, без внешних
зависимостей кроме `jpeg-js`/`pngjs`): из фотографий или видео, снятых вокруг
объекта, строится настоящая 3D-модель в формате GLB.

**Как снимать:** объект на контрастном однотонном фоне, 15–30 кадров по кругу
(360°) с одного уровня и с одинакового расстояния, **против часовой стрелки**
(если модель получится зеркальной — снимали по часовой; либо запустите сервер
с `PMAS_CAPTURE_DIRECTION=cw`). Без рук в кадре, объект не должен касаться
краёв фото. Видео тоже подходит — кадры извлекаются автоматически (нужен
установленный `ffmpeg`).

**Пайплайн движка** (`backend/reconstruction/engine/`):
1. **Силуэты** — фон оценивается по рамке кадра, порог по методу Оцу,
   морфология + связные компоненты + заливка дыр (`silhouette.js`)
2. **Камеры** — turntable-модель: кадры равномерно распределяются по кругу,
   ортографическая проекция (`camera-rig.js`)
3. **Visual hull** — воксельная сетка вырезается по силуэтам всех ракурсов
   (`voxel-carving.js`)
4. **Поверхность** — Surface Nets: водонепроницаемый меш из вокселей
   (`surface-nets.js`)
5. **Постобработка** — сглаживание Таубина, нормали, ориентация наружу
   (`mesh-post.js`)
6. **Цвета** — каждая вершина окрашивается из лучшего по ракурсу фото
   (`vertex-colors.js`)
7. **Экспорт** — бинарный glTF 2.0 (GLB) собственным writer'ом (`glb-writer.js`)

Поддерживается EXIF-ориентация (фото с телефона), три профиля качества
(`fast` / `balanced` / `quality`), прогресс в реальном времени.

**Тесты движка:**
```bash
node backend/reconstruction/engine/selftest.js   # геометрия на эталонных фигурах
node backend/reconstruction/engine/e2e-test.js   # полный цикл через HTTP API
```
Селфтест реконструирует синтетические съёмки сферы и куба и проверяет объём
(сфера: π/6 от bbox, точность ~0.2%), водонепроницаемость и валидность GLB.

**Внешние движки:** через переменные окружения можно подключить COLMAP/Meshroom:
`RECONSTRUCTION_ENGINE_MODE=external_cli RECONSTRUCTION_ENGINE_COMMAND=... npm start`.
Вернуть мок: `RECONSTRUCTION_ENGINE_MODE=mock npm start`.

---

## Технологии

| Компонент | Технология |
|-----------|-----------|
| UI | HTML / CSS / JS |
| AI-детекция | TensorFlow.js + MediaPipe FaceMesh |
| 3D | Three.js + CSS2DRenderer |
| 3D-реконструкция | PMAS Native Engine (visual hull, Node.js) |
| Экспорт PDF | html2canvas + jsPDF |
| Экспорт DOCX | docx.js + FileSaver.js |
| Хранение | localStorage |

---

## Структура проекта

```
PMAS/
├── index.html              # главная страница (2D + 3D + О проекте)
├── start.command            # запуск (macOS)
├── start.bat                # запуск (Windows)
├── package.json             # запуск Node/Express backend scaffold
├── backend/
│   ├── server.js
│   └── reconstruction/      # API 3D Reconstruction
│       └── engine/          # PMAS Native Engine (visual hull → GLB)
├── assets/
│   ├── css/style.css
│   ├── js/app.js            # 3D логика
│   ├── js/01-11*.js         # 2D модули
│   └── img/                 # фото авторов
├── models/
│   ├── LeePerrySmith.glb
│   └── facecap.glb
└── test_face.jpg            # тестовое фото
```

---

## Авторы

| ФИО | Роль |
|------|------|
| Меркулов А.А. | Разработчик |
| Любаев Д.О. | Разработчик |
| Сергеева А.М. | Разработчик |

---

## Лицензия

MIT

**3D-модели:**
- `LeePerrySmith.glb` — [CC-BY 3.0](https://creativecommons.org/licenses/by/3.0/) (Infinite-Realities)
- `facecap.glb` — [MIT](https://github.com/mrdoob/three.js/blob/dev/LICENSE) (Three.js)
